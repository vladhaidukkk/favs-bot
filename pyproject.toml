[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
aiogram = "^3.10.0"

[tool.poetry.group.dev.dependencies]
ruff = "^0.5.4"

[tool.ruff]
line-length = 88
show-fixes = true

[tool.ruff.lint]
extend-select = ["I"]
